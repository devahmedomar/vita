export interface Icart {
  productId: number;
  quantity: number;
  rating: number;
  productPrice: number;
  stockQuantity: number;
  productName: string;
  pictureUrl: string;
  subtotal: number;
}
